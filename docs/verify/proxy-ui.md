# 步骤 14 · 节点代理页验收

日期：2026-09-14。分支 `step/14-proxy-ui`，基线 `a1d324d`（第 13 步）；本地 main 仍为 `6a47aed`。

## 环境与范围

- Windows Node 24，前端生产构建；WSL Ubuntu-24.04 amd64、systemd，真实面板与 Agent，托管 sing-box v1.14.0（第 10 步构建产物）。
- 独立临时 SQLite、JWT、测试管理员和 Agent token；面板仅监听 `127.0.0.1:9014`。首次以 Go 内嵌生产前端测试，收尾改动用独立 Vite `8084` 代理到同一面板复核。
- Browser Use 操作本地页面，桌面及 390×844 视口。原有 `8080/9000` 服务未停止、重启或修改数据。
- 四个测试节点：一个真实在线 Agent；未安装离线节点；用于版本/停止展示的数据库 fixture；已生成目标修订但尚未应用的离线 fixture。fixture 不代表额外真实 VPS。

## 自动检查

在 `web` 执行 `npm test`、`npm run lint`、`npm run build`：12 项测试通过（原有 2 项 + 新增 10 项），全量 lint、TypeScript 和生产构建通过。

新增测试覆盖四协议默认值、按传输避让端口、停用端口保留、端口耗尽/回绕、统计端口保留、Short ID/带宽/端口校验、编辑不回传生成凭据、四种状态、精确修订完成判定、节点用户去重、日志颜色控制符清理。

Go Windows/Linux amd64 内嵌前端构建通过，Linux 版本实际运行。本步未改后端逻辑；第 13 步真实流量与 Go 竞态测试证据继续见 [reconciler.md](reconciler.md)。构建仍有已有的大 chunk 和插件耗时提示，非失败。

## 浏览器与真实链路

| 验收项 | 结果与证据边界 |
|---|---|
| 一览与入口 | 国旗、Agent 状态、四类核心标签、托管版本、可升级提示、入站数、分配人数可见；监控卡片更多菜单直达当前节点代理页。四类状态包含上述 fixture |
| 安装 | 页面确认后真实安装 v1.14.0；约 1.519 s 显示“核心版本已安装”，随后 revision 1 应用并运行。v1.13.0 与托管 v1.14.0 差异显示“升级到 v1.14.0”；真实跨版本升级未在本步执行 |
| 四协议 | 页面分别新建 VLESS 443/tcp、SS 8388/tcp+udp、HY2 8443/udp、TUIC 8444/udp，首次 HY2 自动生成证书；运行态实际监听项包含四协议五项端口 |
| 表单/冲突 | 再次新建 VLESS 建议 444；强填 443 返回“节点的端口和传输协议冲突”，表单保留。编辑协议不可变、公私钥只读、私钥遮蔽；公钥重生后变化。临时 vless-444 删除后恢复四条 |
| 启停与重发 | 关闭 VLESS 后 revision 6/6，443/tcp 消失；再启用后 7/7 并恢复监听。最后一轮独立 API 每 100 ms 观察，从已保存启用到 revision 17 应用实测 6.167 s；页面同步显示修订和监听。手动重新下发显示“目标修订已应用” |
| 失败与日志 | 把 VLESS 改为面板占用的 9014，真实核心启动失败并恢复旧配置；页面显示待应用及最后错误。抽屉读取 `address already in use`，在 10 s 内返回；200/500 行切换、复制、自动滚到底通过；最终版本不显示 ANSI 色码 |
| 修订回滚 | 查看 #7 的只读 JSON，配置顶层字段与接口一致；回滚后产生 #10、来源 rollback:7、哈希等于 #7，页面应用状态 10/10；入站表单仍保留失败的 9014。随后手动改回 443，恢复正常应用 |
| 证书 | 页面指纹与 `openssl x509 -noout -fingerprint -sha256` 一致；确认重生后新旧指纹不同，再次计算仍一致 |
| 人数 | 用已有 API 创建一个测试用户并分配四入站，四条均显示 1 人，总览节点人数为去重后的 1 |
| 离线 | 安装/升级、重启、下发、日志按钮均 disabled，有原因；仍可查看历史及编辑保存配置 |
| 手机 | 390×844 下详情纵向单列、文档宽 390；日志抽屉 x=0/width=390；表单 width=326、height=780，内容 clientHeight=621/scrollHeight=967，内容滚动且保存栏可见；修订表格/JSON 可横向滚动 |

## 清理与未覆盖

验收结束停止本次临时面板、Agent、Vite 和 sing-box 服务，移除本次创建的系统服务文件/核心目录及临时数据库。原项目进程独立保留。

收尾曾中断，恢复后发现临时面板/Agent/Vite 已退出但 WSL 的 sing-box 服务仍存活；核对核心二进制 SHA-256、四个测试入站 tag 和服务 ExecStart 后清理。最终服务 `LoadState=not-found`、`ActiveState=inactive`，五处本次创建的核心/服务路径均不存在。

未覆盖公网 VPS、真实 ufw/firewalld、安全组、ARM64 执行和远端 CI；未重跑第 13 步的 50 MiB 计费实验。本步无新协议、订阅输出或高级 JSON 编辑器，后续分别见步骤 15/17。未配置 Git remote，未推送或合并。
