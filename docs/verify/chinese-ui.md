# 公共界面中文补齐

日期：2026-09-14。用户反馈外观设置抽屉仍有模板英文，说明此前步骤 15–20 的验收漏掉了公共组件文案。本次补充验收不替代目标 VPS、真实外部通知、移动客户端等尚未完成的环境验证。

## 修改范围

- 外观设置：标题、深色模式、高对比度、从右向左、紧凑布局、导航布局与背景样式、主题配色、字体系列、字号、全屏、重置和关闭提示均使用中文。
- 图标选项补充中文可访问名称，字号使用“像素”；抽屉限制最大宽度为视口宽度。设置字段、存储格式和已有选项值保持兼容。
- 全局主题合入已安装的 MUI、Data Grid 和 Date Pickers 简体中文资源，覆盖表格分页、筛选、列管理、导出及日期控件的系统文案。保留现有组件样式和应用覆写顺序。
- 补齐页面搜索及空结果、通知、404、权限提示、公共按钮说明，以及告警通道、订阅凭据、节点令牌和高级配置的界面标签。
- Public Sans、Inter 等字体名称以及 UUID、CPU、Telegram、Webhook 等专有名称保留原名；不改写协议字段、用户数据或服务端返回的原始诊断信息。

## 已完成的检查

- `npm test`：13 项现有测试通过。
- `npm run lint`：全量 ESLint 通过。
- `npm run build`：TypeScript 与生产构建通过；保留原有大型图表分块提示。
- 在生产构建预览中登录现有本地账号：设置抽屉文案、深浅色切换、字号调整及刷新后保留均通过；验收页设置随后恢复。
- 桌面截图及 400 × 850 视口检查：抽屉宽 360，边界位于 40–400，页面宽与内容宽均为 400；字体和字号区域可正常滚动查看。
- 表格筛选字段与操作器、列管理、打印/CSV 导出菜单、分页，页面搜索无结果，节点到期日的中文日期字段与日历导航，以及 404 标题和正文均在浏览器中确认。只打开表单和菜单，未创建节点或提交配置。
- 验收页浏览器错误日志为空。

## 本地运行交付

已将验收后的前端嵌入 Windows 服务端，版本为 `local-steps20-zh`，运行于原来的 `http://localhost:9000`。实际服务端返回的首页和入口 JavaScript 与验收构建逐字节/哈希比对一致，9000 端口的设置抽屉再次经浏览器确认使用中文。

原进程环境配置与数据目录沿用，替换前通过服务端 backup 命令保存一致性备份。更新前签发的会话在更新后仍有效，原账号密码可登录，2 个节点保留、原在线 Agent 已重新连接。健康接口返回成功。

当前程序为 `dist/local-panel/vps-server-zh.exe`；运行信息、备份位置和结果保存在忽略目录的 `runtime.json`、`chinese-ui-verification.json`，日志为 `server-zh.log` 和 `server-zh-error.log`。本次只更新界面资源，未修改数据库迁移或凭据。

仓库未配置 remote，本次交付为本地 main 和本机运行版本。
