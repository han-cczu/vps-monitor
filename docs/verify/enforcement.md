# 订阅限额与到期执行验证

2026-09-14；第16步；纯规则与事务测试运行于Windows Go 1.26。

- `go test ./server/internal/proxy/... ./server/internal/store ./server/cmd/server` 通过。
- Evaluate覆盖手动停用、无限额、恰好达到上限、额度恢复、到期优先、纽约夏令时23小时到期日及UTC等价边界。
- 日历覆盖31日月末钳制、闰年二月、跨年和下次重置日。
- 定时事务覆盖3个同节点用户提醒一次、100%停用通知按节点去重、写失败整体回滚且不发布事件、不清除代理凭据。
- 停机跨月补算到最近一次应重置日；保留历史账期行；重复执行不重复审计；清除旧采样；手动停用不恢复；一次性额度不重置；新账期可再次提醒80%。
- API覆盖加额度立即恢复、改为昨日到期立即停用、清零不恢复已到期用户、手动停用及重新启用评估。
- 统计方向sum → download → sum只对后续落库增量生效；累计计费用量30 → 50 → 80，原始方向量仍是上传30、下载60。

运行装配在Reconciler启动、首条跨日采样前和每分钟统计成功落库后执行检查。月度滚动实现位于`proxy/enforcer.go`，纯日历/规则位于`proxy/enforce`；事务与通知使用已有proxy服务，避免包循环依赖。内部提醒列为`warn80_sent`。每用户`period_date`日期锚防止切时区重复清零，`period_start`仍保留时间戳用于分账；新增0012迁移兼容已经执行0007/0011的数据库。

最终集成已完成：真实Mihomo下载150 MiB触发100 MiB额度停用耗时19.547秒，增加至1 GiB后6.251秒恢复；过期/延长、空订阅、旧链接404均通过。400px订阅列表和总览已点验。详细证据及真机边界见 [completion.md](completion.md)。
